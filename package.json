{
  "name": "tonx86",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm -ws run build",
    "watch": "npm -ws run watch",
    "dev": "npm run watch",
    "test": "npm -ws run test",
    "lint": "npm -ws run lint"
  }
}
